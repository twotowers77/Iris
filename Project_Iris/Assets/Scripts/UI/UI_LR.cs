using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class UI_LR : MonoBehaviour
{
    GameObject noop;

    private Transform trans;
    void Start()
    {
        noop = GetComponent<GameObject>();
        trans = GetComponent<Transform>();
    }

    // Update is called once per frame
    void Update()
    {
        trans
    }
}
